# render.yaml - Versão Final Corrigida

services:
  # Serviço 1: O Banco de Dados PostgreSQL
  - type: pserv
    name: portfolio-db
    plan: free
    postgresMajorVersion: 15 # Esta é a forma correta de especificar a versão

  # Serviço 2: A Aplicação Web (Django)
  - type: web
    name: portfolio-web
    env: python
    plan: free
    buildCommand: |
      pip install --no-cache-dir -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: "gunicorn portfolio_project.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: portfolio-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: "3.11.13"